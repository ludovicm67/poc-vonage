{
  "name": "server",
  "version": "0.1.0",
  "description": "Server part",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && node -r dotenv/config dist/index.js",
    "build": "tsc",
    "dev": "nodemon --watch . --ext json,ts --ignore dist --exec npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^14.3.2",
    "fastify": "^3.27.0",
    "opentok": "^2.12.1",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@types/node": "^17.0.13",
    "@types/opentok": "^2.12.0",
    "nodemon": "^2.0.15"
  }
}
